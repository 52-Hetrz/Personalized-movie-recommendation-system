<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.example.demo.model.User">
        <result column="userId" property="userId" jdbcType="VARCHAR" />
        <result column="userName" property="userName" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="introduction" property="introduction" jdbcType="VARCHAR" />
        <result column="portrait" property="portrait" jdbcType="VARCHAR" />
        <result column="sex" property="sex" jdbcType="VARCHAR" />
        <result column="date" property="date" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="findByUserId" resultType="User">
        SELECT * FROM user WHERE userId=#{userId}

    </select>

    <insert id="insertUser" >
        INSERT INTO user VALUES (#{param1},#{param2},#{param3},#{param4},#{param5},#{param6},#{param7})

    </insert>

    <delete id="deleteUserById" parameterType="java.lang.String">
        DELETE FROM user WHERE userId=#{userId}

    </delete>

    <update id="updateIntroduction">
        UPDATE user SET introduction=#{param2} WHERE userId=#{param1}

    </update>

    <update id="updatePassword" >
        update user set password=#{param2} where userId=#{param1}
    </update>

    <select id="searchPasswordByUserId" resultType="java.lang.String">
        select password from user where userId=#{userId}
    </select>

</mapper>